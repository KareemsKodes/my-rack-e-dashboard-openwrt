# macOS Launchers

These `.command` files are optional double-click shortcuts for common My-Rack-E tasks on macOS.

The main setup app is still launched from the repository root:

```sh
python3 app.py
```

Use these launchers after the MX is running OpenWrt and the repository has been downloaded on the Mac that will manage it.
